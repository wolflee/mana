defmodule Blockchain.Ethash.FNVTest do
  use ExUnit.Case, async: true

  alias Blockchain.Ethash.FNV

  describe "hash/1" do
    test "returns the FNV hash for a list" do
    end

    test "returns the FNV hash for a binary" do
    end
  end
end
